state => data that is connected to the HTML

if a data is converted to state , then any change in data will change the html.

### lifting the state up

- share state between multiple components



### Exercise 
- 3a --> creating an App component and return button with onClick event 
- 3b --> Using React.useState() to save count and updating the count in button
- can not use useState inside the normal function , can only use under components.
